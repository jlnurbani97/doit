services:
  #Database mySQL
  mysql:
    image: mysql:8.0
    container_name: doit-mysql
    restart: always
    env_file:
      - ./.env.docker
    ports:
      - '3307:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
      